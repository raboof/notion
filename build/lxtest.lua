
function x()
    warn(TR("foobar"))

    mod_query.message(TR("Testataan \\vä\"hän \n"
                         .."tätäkin "))
end

-- Some random string that exists in ioncore.
TR('Failed to open input method')

foo.TR('xxx')
